MobileOptimizedButton(
                        onClick = { navController.navigate("counter_selection") },
                        text = "Counter Selection",
                        modifier = Modifier.fillMaxWidth()
                    )

                    MobileOptimizedButton(
                        onClick = { navController.navigate("template_screen") },
                        text = "Receipt Templates",
                        modifier = Modifier.fillMaxWidth()
                    )